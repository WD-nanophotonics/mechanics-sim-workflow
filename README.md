# Mechanics Sim Workflow

Minimal classical-mechanics examples used to exercise the AgentRelay Git workflow.

The first model is a one-dimensional harmonic oscillator acceleration:

\[
a(x) = -\frac{k}{m}x
\]

Run the tests with:

```text
python -m pytest
```
